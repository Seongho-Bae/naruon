import { WorkspacePlaceholderPage } from '@/components/WorkspacePlaceholderPage';

export default function SentPage() {
  return <WorkspacePlaceholderPage eyebrow="Mailbox / Sent" title="보낸 메일 보드 준비 중" description="계정별 reply routing과 sent mailbox ownership이 연결되면 이 화면에서 실제 보낸 메일 흐름을 보여줍니다." />;
}
