import { WorkspacePlaceholderPage } from '@/components/WorkspacePlaceholderPage';

export default function StarredPage() {
  return <WorkspacePlaceholderPage eyebrow="Mailbox / Starred" title="중요 메일 보드 준비 중" description="중요 표시 메일은 mailbox ownership과 실제 folder/star metadata가 연결된 뒤 이 화면에서 필터링됩니다." />;
}
