import { WorkspacePlaceholderPage } from '@/components/WorkspacePlaceholderPage';

export default function AllMailPage() {
  return <WorkspacePlaceholderPage eyebrow="Mailbox / All" title="전체 메일 보드 준비 중" description="전체 메일함은 synthetic multi-mailbox 모델과 중복 메일 collapse 규칙이 구현되면 이 화면과 연결됩니다." />;
}
