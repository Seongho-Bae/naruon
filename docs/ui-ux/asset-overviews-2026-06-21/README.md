# UI/UX Asset Overviews (2026-06-21)

These generated contact sheets render every current UI/UX asset from the
unified repository paths:

- `mockups-contact-sheet.png`: 41 canonical mockup images.
- `reference-set-contact-sheet.png`: 45 dated reference-set images.
- `frontend-branding-alias-contact-sheet.png`: 10 legacy
  `frontend/branding/` images that are exact SHA-256 aliases of canonical
  `docs/ui-ux/mockups/` images.

`manifest.tsv` records the source path, SHA-256 digest, exact alias target when
one exists, and contact sheet path for each rendered asset.

The contact sheets are review aids only. The original PNG files stay as the
source of truth.
