# UI/UX Reference Assets

Store Naruon UI/UX reference images here when they need to survive beyond a
chat session. Keep dated subdirectories and filenames stable enough for design
review comments.

The durable 45-image UI/UX reference set lives in
`reference-set-2026-06-18/images/`. Its `sources.tsv` file records each image's
stable repository path and SHA-256 digest.
